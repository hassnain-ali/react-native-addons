///
/// HybridClipboardSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif



#include <string>
#include <vector>
#include <functional>

namespace margelo::nitro::addons {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `Clipboard`
   * Inherit this class to create instances of `HybridClipboardSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridClipboard: public HybridClipboardSpec {
   * public:
   *   HybridClipboard(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridClipboardSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridClipboardSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridClipboardSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::string getString() = 0;
      virtual std::vector<std::string> getStrings() = 0;
      virtual std::string getImagePNG() = 0;
      virtual std::string getImageJPG() = 0;
      virtual void setImage(const std::string& content) = 0;
      virtual std::string getImage() = 0;
      virtual void setString(const std::string& content) = 0;
      virtual void setStrings(const std::vector<std::string>& content) = 0;
      virtual bool hasString() = 0;
      virtual bool hasImage() = 0;
      virtual bool hasURL() = 0;
      virtual bool hasNumber() = 0;
      virtual bool hasWebURL() = 0;
      virtual void onClipboardChanged(const std::function<void()>& callback) = 0;
      virtual void removeAllListeners() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "Clipboard";
  };

} // namespace margelo::nitro::addons
